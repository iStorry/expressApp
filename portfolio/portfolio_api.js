const router = require('express').Router();
const models = require('./portfolio_models')

router.get("/", (req, res) => {
})

router.get("/holdings", (req, res) => {
})

router.get("/returns", (req, res) => {
})

module.exports = {
    router
}